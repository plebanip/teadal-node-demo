apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: minio
  name: minio
spec:
  type: NodePort
  ports:
  - name: "9000"
    port: 9000
    targetPort: 9000
    nodePort: 30080
  - name: "console"
    port: 9001
    targetPort: 9001
    nodePort: 30091 

  selector:
    io.kompose.service: minio
status:
  loadBalancer: {}
