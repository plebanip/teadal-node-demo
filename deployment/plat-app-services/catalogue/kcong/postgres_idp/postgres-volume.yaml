apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-idp-volume1
  labels:
    io.kompose.service: postgres-idp-claim1
spec:
  capacity:
    storage: 500Mi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /mnt/data/postgres-idp-volume1
